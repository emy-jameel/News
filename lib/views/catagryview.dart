import 'package:flutter/material.dart';

class CatagryView extends StatelessWidget {
  const CatagryView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SizedBox()
      ,);
  }
}